{
  "cohort_id": "YOUR_COHORT_NAME_HERE",
  "cohort_changes": [
    {
      "user_ids": [
        {% for row in rows %}
        "{{ row['user_id'] }}"{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ],
      "braze_user_ids": [
        {% for row in rows %}
        "{{ row['braze_user_id_latest'] }}"{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ]
    }
  ]
}
