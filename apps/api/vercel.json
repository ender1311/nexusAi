{
  "framework": null,
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "npx prisma generate --config prisma.config.ts && npx esbuild api/_index.ts --bundle --platform=node --format=cjs --outfile=api/index.js --packages=external",
  "outputDirectory": ".",
  "build": {
    "env": {
      "HUSKY": "0"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
